<h1>New Kr</h1>

<%= form_for ([@team, @okr, @okr.krs.build]) do |f| %>
  
  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :howtoscore %>
    <%= f.text_area :howtoscore %>
  </div>

  <div class="field">        
    <%= f.hidden_field :okr_id %>
    <%= f.hidden_field :okr_id%>        
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= link_to 'Back', team_okr_krs_path %>
